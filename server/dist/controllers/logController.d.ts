import { Request, Response } from 'express';
export declare class LogController {
    getUserLogs(req: Request, res: Response): void;
    getUserLog(req: Request, res: Response): void;
    deleteUserLogs(req: Request, res: Response): void;
}
